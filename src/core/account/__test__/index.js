const { getRelevantCoinOrder } = require('./');

const { prepareLastOrder } = require('./');

describe('getRelevantCoinOrder', () => {
    describe('run LTC-USD-1 scenario', () => {
        const matches = require('./tapes/LTC-USD-1.json');
        const currency = 'USD';
        balance = 0.00000001;
    });
});
