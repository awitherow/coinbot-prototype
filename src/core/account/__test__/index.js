const { getRelevantCoinOrder } = require('./');

describe('getRelevantCoinOrder', () => {
    describe('run BTC-USD-1 scenario', () => {
        const matches = require('./tapes/BTC-USD-1.json');
        const currency = 'USD';
        balance = 0.00000001;
    });
});
